#include<bits/stdc++.h>
using namespace std;
long long calculate(vector<vector<int>>&arr, int n, long long mod){
	long long ans =0;
	
}

bool comp(, int b){
	
}
int main(){
	int t;
	long long mod = 1000000007;
	cin>>t;
	while(t--){
		int n;
		cin>>n;
		vector<vector<pair<int,int>>>arr(3,vector<pair<int,int>>arr(n));
		int x;
		for(int i=0; i<3; i++){
			for(int j=0; j<n; j++)
				cin>>x;
				arr[i][j] = make_pair(x,j);
		}
		
		for(int i=0; i<n; i++){
			
		}
		sort(arr[0].begin(),arr[0].end(),comp);
		sort();
		sort();
		vector<long long>st(n,0);	
	}
	return 0;
}
